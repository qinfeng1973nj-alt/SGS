# v0.2.4 — 测试升级与环境规范

## 变更摘要
- 新增 `.gitattributes`，统一跨平台行尾策略（LF 为主）。
- 新增 `tests/conftest.py`，提供 FastAPI `TestClient` fixture。
- 将 `tests/test_error_envelope.py` 从静态契约校验升级为真实 API 集成测试。
- 使用 `/score` 非法请求触发错误响应，并按 `schemas/error_envelope.schema.json` 校验。

## 价值
- 测试从“静态结构检查”升级为“运行时 HTTP 契约验证”。
- 降低 Windows/Linux 协作中的 EOL 噪音与无效 diff。

## 验证
- `pytest -q tests/test_error_envelope.py -vv` 通过
- `pytest -q` 通过
